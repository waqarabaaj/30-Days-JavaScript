// Define variables for property names
const firstNameKey = "firstName";
const lastNameKey = "lastName";

// Define an object with computed property names
const person = {
  [firstNameKey]: "John",
  [lastNameKey]: "Doe",
  age: 30,
};

// Log the object to the console
console.log(person);
