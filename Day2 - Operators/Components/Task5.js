export default function () {
  // Program to find reminder of two numbers and show result in console log

  let dividend = 10;
  let divisor = 3;

  let remainder = dividend % divisor;

  console.log(remainder); // Output: 1
}
