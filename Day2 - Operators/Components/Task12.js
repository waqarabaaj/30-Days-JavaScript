export default function () {
  // Write a program that uses || operator to combine two condations and log the result to the console
  let condation1 = true;
  let condation2 = false;

  let result = condation1 || condation2;

  console.log("The result of condation1 || condation2 is", result);
}
