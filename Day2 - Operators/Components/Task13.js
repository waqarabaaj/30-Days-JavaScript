export default function () {
  // Write a program that uses ! operator to combine two condations and log the result to the console
  let condation1 = true;

  let result = !condation1;

  console.log("The result of condation1", result);
}
